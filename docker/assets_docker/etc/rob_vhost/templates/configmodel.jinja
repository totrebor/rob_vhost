<VirtualHost {{ vh_ip}}:80>
 ServerName {{ vh_hostname }}
 ServerAdmin support@progettosl.eu
 DocumentRoot {{ hostingDir}}
 ErrorLog ${APACHE_LOG_DIR}/{{ vh_code }}-{{ vh_hostname }}/error.log
 CustomLog ${APACHE_LOG_DIR}/{{ vh_code }}-{{ vh_hostname }}/access.log combined
# <FilesMatch ".+\.ph(ar|p|tml)$">
#  <If "-f %{REQUEST_FILENAME}">
#   SetHandler "proxy:unix:/run/php/php7.4-fpm.sock|fcgi://localhost"
#  </If>
# </FilesMatch>
</VirtualHost>

#<VirtualHost {{ vh_ip}}:443>
# ServerName {{ vh_hostname }}
# ServerAdmin support@progettosl.eu
# DocumentRoot {{ hostingDir}}
# ErrorLog ${APACHE_LOG_DIR}/{{ vh_code }}-{{ vh_hostname }}/error.log
# CustomLog ${APACHE_LOG_DIR}/{{ vh_code }}-{{ vh_hostname }}/access.log combined
#
# SSLEngine on
# SSLCertificateFile /etc/letsencrypt/live/{{ vh_hostname }}/fullchain.pem
# SSLCertificateKeyFile /etc/letsencrypt/live/{{ vh_hostname }}/privkey.pem
# SSLCertificateChainFile /etc/letsencrypt/live/{{ vh_hostname }}/chain.pem
# Include /etc/letsencrypt/options-ssl-apache.conf
#
# <FilesMatch ".+\.ph(ar|p|tml)$">
#  <If "-f %{REQUEST_FILENAME}">
#   SetHandler "proxy:fcgi://php7fpm:9000"
#  </If>
# </FilesMatch>
#</VirtualHost>
